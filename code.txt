# code.txt
import IO;

function main(List of String args): Void
{
	Integer number;
	number = io.stdin.getln(); # Get user input
	IO.stdout.writeln(number);
	if(number == 42 and true != false)
	{

	}
	foreach(args as String arg)
	{
		IO.stdout.writeln(arg);
	}
	Integer number2 = -2;
	String stringtest = "TestString";
}
